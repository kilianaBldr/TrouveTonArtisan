import { TopArtisanPipe } from './top-artisan.pipe';

describe('TopArtisanPipe', () => {
  it('create an instance', () => {
    const pipe = new TopArtisanPipe();
    expect(pipe).toBeTruthy();
  });
});
